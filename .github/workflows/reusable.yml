name: Reusable Workflow

on:
  workflow_call:
    inputs:
      who-to-greet:
        type: string
        default: 'World'

jobs:
  reusable-job:
    runs-on: ubuntu-latest
    steps:
      - name: Greet someone
        run: echo 'Hello'
